# grammar-expressions Rust package

Rust library and command-line interface for the grammar-expressions engine.

```sh
cargo test -p grammar-expressions
cargo run -p grammar-expressions -- match "{word:[A-Za-z]+}" "grammar"
```
