# grammar-expressions JavaScript package

JavaScript library, command-line interface, and JSON HTTP microservice for the
grammar-expressions engine.

```sh
npm test --prefix js
node js/bin/grammar-expressions.js match "{word:[A-Za-z]+}" "grammar"
```
